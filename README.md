# Kansliet (Design Company)

Next.js site for Kansliet — portfolio, works, studio, and contact.

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Copy `.env.example` to `.env.local` and add your keys.

## Deploy to Vercel

See **[docs/DEPLOY-VERCEL.md](docs/DEPLOY-VERCEL.md)** for step-by-step: push to GitHub, import in Vercel, set env vars, add domain kansliet.co.
